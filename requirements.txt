Flask
gunicorn
Werkzeug
reportlab
Pillow
pytesseract
pypdf
PyMuPDF
requests

# Also install the Tesseract OCR app on your computer:
# macOS: brew install tesseract
# Ubuntu/Debian: sudo apt install tesseract-ocr
# Windows: install Tesseract OCR, then add it to PATH